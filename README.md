markdown
# Modular Architecture API (Feature-Based Structure)

[![FastAPI](https://img.shields.io/badge/FastAPI-005571?style=for-the-badge&logo=fastapi)](https://fastapi.tiangolo.com)
[![Python](https://img.shields.io/badge/Python-3.9+-blue?style=for-the-badge&logo=python)](https://python.org)

Этот проект представляет собой **шаблон модульной архитектуры** на базе FastAPI, где код организован по функциональным возможностям (фичам) или бизнес-доменам, а не по техническим слоям.

## 🧩 Что такое модульная архитектура?

Модульная структура (также называемая структурой по доменам или фичам) — это подход к организации кодовой базы, при котором код разбивается на относительно независимые модули, каждый из которых отвечает за определенную функциональную область приложения.

В отличие от монолитной структуры, где код группируется по техническим слоям (все модели в одной папке, все роутеры в другой), модульный подход группирует **весь код для конкретной функции в одном месте**.
```text
┌─────────────────────────────────────────────┐
│ Монолитная структура │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ │
│ │ models/ │ │routes/ │ │schemas/ │ │
│ │ user.py│ │ user.py │ │ user.py │ │
│ │ post.py│ │ post.py │ │ post.py │ │
│ └─────────┘ └─────────┘ └─────────┘ │
└─────────────────────────────────────────────┘

                ↓
┌─────────────────────────────────────────────┐
│ Модульная структура │
│ ┌──────────────────┐ ┌──────────────────┐ │
│ │ Модуль Users │ │ Модуль Posts │ │
│ │ ┌─────────────┐ │ │ ┌─────────────┐ │ │
│ │ │ routers.py │ │ │ │ routers.py │ │ │
│ │ │ schemas.py │ │ │ │ schemas.py │ │ │
│ │ │ models.py │ │ │ │ models.py │ │ │
│ │ │ crud.py │ │ │ │ crud.py │ │ │
│ │ └─────────────┘ │ │ └─────────────┘ │ │
│ └──────────────────┘ └──────────────────┘ │
└─────────────────────────────────────────────┘
```

## 🎯 Ключевые принципы

| Принцип | Описание |
| :--- | :--- |
| **Разделение по функциональности** | Весь код, относящийся к определенной бизнес-сущности (пользователи, посты, заказы), находится в одном модуле. |
| **APIRouter** | Каждый модуль имеет свой экземпляр `APIRouter`, который определяет эндпоинты только для своей функциональности. |
| **Инкапсуляция** | Модули максимально независимы. Взаимодействие происходит через четко определенные интерфейсы. |
| **Единая ответственность** | Каждый модуль отвечает за полный набор операций, связанных с его функциональностью. |

## 🗂️ Структура проекта
```text
module/
├── app/
│ ├── init.py
│ ├── main.py # Главный файл приложения (подключает модули)
│ │
│ ├── core/ # 🔧 Общие компоненты для всех модулей
│ │ ├── init.py
│ │ ├── database.py # Настройка подключения к БД
│ │ ├── dependencies.py # Общие зависимости (сессия БД, аутентификация)
│ │ └── config.py # Общие настройки приложения
│ │
│ └── modules/ # 📦 Функциональные модули
│ │
│ ├── categories/ # Модуль "Категории"
│ │ ├── init.py
│ │ ├── routers.py # Эндпоинты для категорий
│ │ ├── schemas.py # Pydantic схемы категорий
│ │ ├── models.py # SQLAlchemy модели категорий
│ │ └── crud.py # CRUD операции для категорий
│ │
│ └── posts/ # Модуль "Посты"
│ ├── init.py
│ ├── routers.py # Эндпоинты для постов
│ ├── schemas.py # Pydantic схемы постов
│ ├── models.py # SQLAlchemy модели постов
│ └── crud.py # CRUD операции для постов
│
└── requirements.txt
```

## 📦 Содержимое модуля

Каждый модуль представляет собой самодостаточный "мини-проект" внутри основного приложения:
```text
modules/categories/ # Модуль категорий
├── init.py
├── routers.py # API эндпоинты (GET, POST, PUT, DELETE для /categories)
├── schemas.py # Pydantic модели для валидации ввода/вывода
├── models.py # SQLAlchemy модели для БД
└── crud.py # Бизнес-логика и операции с БД
```

## 🔄 Взаимодействие компонентов

1. **main.py** — минималистичный файл, который только создает приложение и подключает роутеры модулей:
```python
from fastapi import FastAPI
from app.modules.categories.routers import router as categories_router
from app.modules.posts.routers import router as posts_router

app = FastAPI()
app.include_router(categories_router, prefix="/api/v1/categories", tags=["categories"])
app.include_router(posts_router, prefix="/api/v1/posts", tags=["posts"])
```
core/dependencies.py — содержит общие зависимости (например, получение сессии БД), которые используются во всех модулях.

Модули — каждый модуль работает независимо, используя общие компоненты из core.

## ✨ Преимущества модульной архитектуры

✅ Для разработки
Понятная навигация — все файлы, относящиеся к одной фиче, рядом

Меньше конфликтов в Git — разные команды работают в разных модулях

Более легкий код-ревью — изменения изолированы в одном модуле

✅ Для поддержки
Локализация изменений — правки в категориях не затрагивают посты

Упрощенный рефакторинг — можно полностью переписать модуль, не трогая остальные

Тестирование — каждый модуль тестируется отдельно

✅ Для масштабирования
Готовность к микросервисам — модуль легко вынести в отдельный сервис

Параллельная разработка — несколько команд могут работать над разными модулями одновременно

Независимое развертывание — в будущем модули можно деплоить отдельно

## 🚀 Быстрый старт

Клонируйте репозиторий:

```bash
git clone https://github.com/MaminAmetist/module.git
cd module
```

Создайте и активируйте виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows
```

Установите зависимости:

```bash
pip install -r requirements.txt
```

Запустите сервер:

```bash
uvicorn app.main:app --reload
```

Изучите документацию:
Перейдите по адресу http://127.0.0.1:8000/docs

## 📖 Когда выбирать модульную архитектуру?
Модульная структура особенно полезна в следующих случаях:

Ситуация |	Почему подходит
| :--- | :--- |
Средние и крупные проекты |	Легко масштабировать и поддерживать
Командная разработка |	Команды работают над разными модулями параллельно
Продукт с четкими доменами	| Пользователи, товары, заказы — естественные модули
Потенциальный переход на микросервисы |	Модули — идеальные кандидаты для выделения

## ⚖️ Сравнение с другими архитектурами

| Характеристика |	Монолитная |	Модульная |	Луковая
| :--- | :---: | :---: | :---: |
Организация кода |	По техническим слоям |	По фичам/доменам |	По уровням абстракции
Связанность |	Высокая |	Низкая |	Очень низкая
Простота старта |	⭐⭐⭐ |	⭐⭐ |	⭐
Масштабируемость |	⭐ |	⭐⭐⭐ |	⭐⭐⭐
Поддержка в долгосрок |	⭐ |	⭐⭐⭐ |	⭐⭐⭐⭐

## 🎓 Заключение

Модульная структура по функциональным возможностям является рекомендуемым подходом для средних и крупных проектов на FastAPI. Она значительно улучшает организацию кода, облегчает командную разработку и подготавливает проект к возможному переходу на микросервисную архитектуру в будущем.

Главный принцип: группируй код по тому, ЧТО он делает (бизнес-функция), а не по тому, КАК он это делает (техническая роль).
